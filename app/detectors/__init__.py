"""Detector modules — one file per signal source, each independently testable.

FREE (run on every domain): ct_logs, dns, fingerprint, careers.
PAID (gated, cached, capped via app.ledger): apollo, parallel.

Every detector reads PUBLIC data only. See the LEGAL LINE in CLAUDE.md.
"""
